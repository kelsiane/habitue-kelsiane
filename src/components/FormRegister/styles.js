import styled from "styled-components";

export const Form = styled.div`
  width: 400px;
  display: flex;
  flex-direction: column;
  h2 {
    font-size: 2rem;
    text-align: center;
    color: var(--black);
  }
  gap: 0.8rem;
  #btn {
    margin: 0 0 0 auto;
  }
`;
